apiVersion: v1
kind: Service
metadata:
  name: test-app-svc
  labels:
    app: test-app
spec:
  selector:
    app: test-app
  ports:
    - name: http
      port: 80        # порт сервиса внутри кластера
      targetPort: 8080 # порт контейнера
  type: ClusterIP
